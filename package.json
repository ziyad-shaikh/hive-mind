{
  "name": "hive-mind",
  "displayName": "Hive Mind - Code Dependency Graph",
  "description": "Visualizes code dependencies as an interactive Obsidian-style graph and provides Copilot tools to analyze change impact across your codebase.",
  "version": "0.1.0",
  "publisher": "hive-mind",
  "engines": {
    "vscode": "^1.87.0"
  },
  "categories": ["Other", "Visualization"],
  "keywords": ["dependency", "graph", "copilot", "impact", "visualization", "architecture"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "hivemind.hivemind",
        "name": "hivemind",
        "fullName": "Hive Mind",
        "description": "Ask about code dependencies, impact analysis, and related files",
        "isSticky": true,
        "commands": [
          {
            "name": "impact",
            "description": "Analyze which files are affected if the current file changes"
          },
          {
            "name": "deps",
            "description": "Show dependencies of the current file"
          },
          {
            "name": "cycles",
            "description": "Detect circular dependencies in the workspace"
          }
        ],
        "disambiguation": [
          {
            "category": "dependency-analysis",
            "description": "The user wants to understand code dependencies, imports, file relationships, or how files connect to each other in the codebase.",
            "examples": [
              "What files does this file depend on?",
              "Show me the dependency graph",
              "What imports does this module have?",
              "Which files are related to this one?"
            ]
          },
          {
            "category": "impact-analysis",
            "description": "The user wants to know which files would be affected by changing a specific file, or wants to understand the downstream impact of a code change.",
            "examples": [
              "What would break if I change this file?",
              "Which files depend on this module?",
              "What is the impact of modifying this code?",
              "Show me what files I need to update if I change this"
            ]
          },
          {
            "category": "circular-dependencies",
            "description": "The user wants to find circular or cyclic dependencies in the codebase.",
            "examples": [
              "Are there any circular dependencies?",
              "Find import cycles in my project",
              "Show me cyclic dependencies"
            ]
          }
        ]
      }
    ],
    "commands": [
      {
        "command": "hiveMind.showGraph",
        "title": "Hive Mind: Show Full Code Graph"
      },
      {
        "command": "hiveMind.showFileGraph",
        "title": "Hive Mind: Focus Graph on Current File"
      },
      {
        "command": "hiveMind.analyzeWorkspace",
        "title": "Hive Mind: Re-analyze Workspace"
      },
      {
        "command": "hiveMind.showIndexedFiles",
        "title": "Hive Mind: Show Indexed Files"
      },
      {
        "command": "hiveMind.showCycles",
        "title": "Hive Mind: Show Circular Dependencies"
      },
      {
        "command": "hiveMind.showStats",
        "title": "Hive Mind: Show Workspace Stats"
      },
      {
        "command": "hiveMind.scaffoldInstructions",
        "title": "Hive Mind: Scaffold AI Instructions & Skills"
      },
      {
        "command": "hiveMind.checkClangd",
        "title": "Hive Mind: Check clangd Status"
      },
      {
        "command": "hiveMind.invalidateCache",
        "title": "Hive Mind: Invalidate Graph Cache & Reindex"
      },
      {
        "command": "hiveMind.showCoChangeHeatMap",
        "title": "Hive Mind: Show Co-Change Heat Map"
      },
      {
        "command": "hiveMind.refreshTree",
        "title": "Refresh",
        "icon": "$(refresh)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "hiveMind.showFileGraph",
          "group": "navigation",
          "when": "resourceScheme == file"
        },
        {
          "command": "hiveMind.showCoChangeHeatMap",
          "group": "navigation",
          "when": "resourceScheme == file"
        }
      ],
      "explorer/context": [
        {
          "command": "hiveMind.showFileGraph",
          "group": "hivemind"
        },
        {
          "command": "hiveMind.showCoChangeHeatMap",
          "group": "hivemind"
        }
      ],
      "view/title": [
        {
          "command": "hiveMind.refreshTree",
          "when": "view == hiveMindDependencyTree",
          "group": "navigation"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "hiveMindExplorer",
          "title": "Hive Mind",
          "icon": "$(pulse)"
        }
      ]
    },
    "views": {
      "hiveMindExplorer": [
        {
          "id": "hiveMindDependencyTree",
          "name": "Dependencies",
          "contextualTitle": "Hive Mind Dependencies"
        }
      ]
    },
    "configuration": {
      "title": "Hive Mind",
      "properties": {
        "hiveMind.maxFiles": {
          "type": "number",
          "default": 5000,
          "minimum": 100,
          "maximum": 50000,
          "description": "Maximum number of source files to index. Increase for very large workspaces."
        },
        "hiveMind.ignoredDirectories": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Additional directory names to exclude from analysis (on top of the built-in list like node_modules, .git, dist, etc.)."
        },
        "hivemind.clangdPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to a clangd executable. If empty, Hive Mind auto-detects clangd from the vscode-clangd extension, PATH, or common LLVM install locations. Used for AST-precise C/C++ analysis (find references, overrides, call hierarchy)."
        },
        "hivemind.clangPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to a clang executable (the compiler driver, separate from clangd). If empty, Hive Mind auto-detects clang from the vscode-clangd install, PATH, or common LLVM install locations. Used by hivemind_macroExpand to run the C/C++ preprocessor."
        },
        "hiveMind.respectIfdefBranches": {
          "type": "boolean",
          "default": true,
          "description": "When true, Hive Mind strips #include directives that lie in dead #ifdef/#if branches based on -D flags from compile_commands.json. Set to false to revert to the legacy behaviour of treating every #include as a real edge (more edges, more false-positive cycles, but no risk of dropping a real edge)."
        }
      }
    },
    "languageModelTools": [
      {
        "name": "hivemind_getDependencies",
        "toolReferenceName": "hivemindDeps",
        "tags": ["hivemind", "dependencies", "imports", "code-graph"],
        "displayName": "Get File Dependencies",
        "modelDescription": "Returns all files that a given source file imports or depends on (direct and transitive). Use this to understand what a file relies on before modifying it.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the file — relative to workspace root or absolute. Fuzzy matching is supported (e.g. just the filename)."
            },
            "depth": {
              "type": "number",
              "description": "Traversal depth. 1 = direct only, -1 = all transitive. Defaults to 2."
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_getImpact",
        "toolReferenceName": "hivemindImpact",
        "tags": ["hivemind", "impact", "change", "code-graph"],
        "displayName": "Get Change Impact",
        "modelDescription": "Given a file being modified, returns all other files that import or depend on it — i.e., files that may need to be updated as a result of the change. Always call this when editing a file to check for downstream impact.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the file being changed — relative to workspace root or absolute"
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_getRelatedFiles",
        "toolReferenceName": "hivemindRelated",
        "tags": ["hivemind", "related", "connections", "code-graph"],
        "displayName": "Get Related Files",
        "modelDescription": "Returns all files directly connected to a given file in the dependency graph — both what it imports and what imports it. Use to get a complete picture of a file's role in the codebase.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the file"
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_getFullGraph",
        "toolReferenceName": "hivemindGraph",
        "tags": ["hivemind", "graph", "architecture", "code-graph"],
        "displayName": "Get Full Dependency Graph",
        "modelDescription": "Returns the complete dependency graph of the workspace as structured data including hub files, language breakdown, and circular dependencies. Use this to understand overall architecture.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "maxNodes": {
              "type": "number",
              "description": "Maximum number of nodes to return. Defaults to 150."
            }
          }
        }
      },
      {
        "name": "hivemind_detectCycles",
        "toolReferenceName": "hivemindCycles",
        "tags": ["hivemind", "circular", "cycle", "code-graph"],
        "displayName": "Detect Circular Dependencies",
        "modelDescription": "Detects and returns all circular dependency chains in the workspace. Use when investigating build issues, module coupling, or architecture health.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "hivemind_getTestFiles",
        "toolReferenceName": "hivemindTests",
        "tags": ["hivemind", "test", "testing", "code-graph"],
        "displayName": "Get Related Test Files",
        "modelDescription": "Given a source file, returns its associated test files (e.g. foo.test.ts for foo.ts). Given a test file, returns the source file it tests. Use this to find tests that should be updated when modifying source code.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the source or test file"
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_findSymbol",
        "toolReferenceName": "hivemindSymbol",
        "tags": ["hivemind", "symbol", "function", "class", "code-graph"],
        "displayName": "Find Symbol Across Workspace",
        "modelDescription": "Searches for a function, class, interface, type, or variable by name across all indexed workspace files. Returns file locations and symbol kinds. Use this to find where a symbol is defined.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "symbolName": {
              "type": "string",
              "description": "The name of the symbol to search for (function, class, type, variable, etc.)"
            }
          },
          "required": ["symbolName"]
        }
      },
      {
        "name": "hivemind_coChanged",
        "toolReferenceName": "hivemindCoChanged",
        "tags": ["hivemind", "git", "history", "coupling", "code-graph"],
        "displayName": "Get Co-Changed Files",
        "modelDescription": "Returns files that frequently change together with the given file based on git history. These are historically coupled files that may need to be updated together even without direct import relationships.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the file to find co-changed files for"
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_getContext",
        "toolReferenceName": "hivemindContext",
        "tags": ["hivemind", "context", "bundle", "code-graph", "ai"],
        "displayName": "Get Curated Context Bundle",
        "modelDescription": "Returns a comprehensive context bundle for a file: its dependencies, dependents, test files, co-changed files, and exported symbols — all in a single call. Use this FIRST when starting work on any file to understand its role and connections. Optionally include the file's source content. This is the recommended starting tool — it replaces calling getDependencies, getImpact, getTestFiles, coChanged, and findSymbol separately.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the file — relative to workspace root or absolute"
            },
            "task": {
              "type": "string",
              "description": "Optional description of what you're trying to do. Helps frame the context."
            },
            "includeContent": {
              "type": "boolean",
              "description": "If true, includes the file's source code (capped at 200 lines). Default false."
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_planChange",
        "toolReferenceName": "hivemindPlan",
        "tags": ["hivemind", "plan", "change", "impact", "code-graph", "ai"],
        "displayName": "Plan a Code Change",
        "modelDescription": "Given a file and a description of intended changes, returns a prioritized plan: which files to modify, which to read for context, which tests to run, and which files are peripherally affected. Scores files by structural dependencies, test relationships, and git co-change history. Use this BEFORE making multi-file changes to avoid missing impacted files.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the primary file being changed"
            },
            "description": {
              "type": "string",
              "description": "What change is being made — e.g. 'rename the handleRequest method' or 'add a new parameter to the constructor'"
            }
          },
          "required": ["filePath", "description"]
        }
      },
      {
        "name": "hivemind_search",
        "toolReferenceName": "hivemindSearch",
        "tags": ["hivemind", "search", "find", "grep", "text", "code-graph"],
        "displayName": "Structural-Aware Code Search",
        "modelDescription": "Searches the entire workspace for text or regex patterns, then ranks results by combining match count with structural signals from the dependency graph. Files whose graph neighbors also match (cluster signal), files near a contextFile, hub files, and files with matching exported symbols are boosted. Use this instead of raw grep when you want the most relevant files first — not just files with the most hits. Supports an optional contextFile to bias results toward a specific area of the codebase.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "The text or regex pattern to search for across all workspace files"
            },
            "isRegex": {
              "type": "boolean",
              "description": "If true, treat query as a regular expression. Default false."
            },
            "caseSensitive": {
              "type": "boolean",
              "description": "If true, match case. Default false (case-insensitive)."
            },
            "maxResults": {
              "type": "number",
              "description": "Maximum number of files to return. Default 30."
            },
            "contextFile": {
              "type": "string",
              "description": "Optional file path to bias results toward. Files structurally connected to this file get boosted in ranking."
            },
            "includeSnippets": {
              "type": "boolean",
              "description": "If true, include code snippets from top matches. Default true."
            }
          },
          "required": ["query"]
        }
      },
      {
        "name": "hivemind_getCppPair",
        "toolReferenceName": "hivemindCppPair",
        "tags": ["hivemind", "cpp", "c++", "header", "source", "refactor", "code-graph"],
        "displayName": "Get C/C++ Header/Source Pair",
        "modelDescription": "Given a C/C++ file (.h, .hpp, .cpp, .cc, .inl, .ipp, etc.), returns the matching header/source/inline files by basename match. Use this BEFORE editing any C++ file — modifying a function in foo.cpp almost always requires updating its declaration in foo.h. Skipping this is the #1 cause of broken C++ refactors.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to a C/C++ file — relative to workspace root or absolute"
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_findMacro",
        "toolReferenceName": "hivemindMacro",
        "tags": ["hivemind", "cpp", "c++", "macro", "preprocessor", "define", "code-graph"],
        "displayName": "Find C/C++ Macro Definitions",
        "modelDescription": "Locates `#define` macro definitions across all C/C++ files in the workspace. Returns ONLY definitions (not usages) — distinguishes object-like vs function-like macros, shows the body, and reports multiple definitions when present (e.g. platform-gated variants). Use this whenever you encounter a SCREAMING_SNAKE_CASE token in C/C++ code and aren't sure if it's a macro, before assuming what it expands to. Critical for codebases like FCIMPL/QCALL/HCIMPL/BEGIN_PINVOKE-style macro-heavy frameworks.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "Macro name to find (e.g. 'FCIMPL3', 'STATIC_CONTRACT_NOTHROW'). By default an exact match — set exact:false for substring search."
            },
            "exact": {
              "type": "boolean",
              "description": "If true (default), exact name match. If false, substring match — useful when you only know part of the macro name."
            },
            "maxResults": {
              "type": "number",
              "description": "Maximum number of definitions to return. Default 25."
            }
          },
          "required": ["name"]
        }
      },
      {
        "name": "hivemind_findReferences",
        "toolReferenceName": "hivemindRefs",
        "tags": ["hivemind", "cpp", "c++", "references", "refactor", "clangd", "ast", "code-graph"],
        "displayName": "Find C/C++ References (AST-precise)",
        "modelDescription": "Returns every place a C/C++ symbol is *actually* referenced — AST-precise via clangd, NOT text grep. Resolves overload sets, namespaces, virtual dispatch, templates, and respects the active #ifdef configuration from compile_commands.json. Use this BEFORE renaming any C/C++ function, class, or variable — it's the only way to guarantee you find every call site without false positives from comments, strings, or unrelated symbols with the same name. Provide either `symbolName` (most common) or an explicit `line` + `character` cursor position. Falls back gracefully with a clear error if clangd is unavailable.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the C/C++ file containing (or near) the symbol. Used to anchor clangd at a specific translation unit."
            },
            "symbolName": {
              "type": "string",
              "description": "Name of the symbol to find references for (e.g. 'doFoo', 'MyClass::bar', 'kMaxSize'). Hive Mind locates the symbol's declaration in the file via clangd's documentSymbols, then queries references at that position. Either this OR (line + character) is required."
            },
            "line": {
              "type": "number",
              "description": "1-indexed line number of the cursor position. Use when the symbol name is ambiguous in the file. Required together with `character`."
            },
            "character": {
              "type": "number",
              "description": "0-indexed column number of the cursor position (LSP convention). Required together with `line`."
            },
            "includeDeclaration": {
              "type": "boolean",
              "description": "If true (default), include the declaration site in results. Set false to see only call sites / usages."
            },
            "maxResults": {
              "type": "number",
              "description": "Maximum number of references to display. Default 100."
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_findOverrides",
        "toolReferenceName": "hivemindOverrides",
        "tags": ["hivemind", "cpp", "c++", "virtual", "override", "polymorphism", "clangd", "ast", "code-graph"],
        "displayName": "Find C/C++ Virtual Overrides (AST-precise)",
        "modelDescription": "For a C/C++ virtual method, returns every concrete implementation across the codebase via clangd's `textDocument/implementation`. AST-precise — handles multiple inheritance, covariant return types, and override chains correctly. Use BEFORE modifying a virtual method's signature, behavior contract, or visibility — every override returned must be updated in lockstep. Closes the virtual-dispatch blind spot that grep cannot solve. Provide either `symbolName` (the virtual method's name) or an explicit `line` + `character` cursor position.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the C/C++ file containing the virtual method (typically the base class header)."
            },
            "symbolName": {
              "type": "string",
              "description": "Name of the virtual method (e.g. 'doWork', 'IFoo::bar'). Either this OR (line + character) is required."
            },
            "line": {
              "type": "number",
              "description": "1-indexed line number of the cursor position. Required together with `character`."
            },
            "character": {
              "type": "number",
              "description": "0-indexed column number of the cursor position (LSP convention). Required together with `line`."
            },
            "maxResults": {
              "type": "number",
              "description": "Maximum number of overrides to display. Default 100."
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_callHierarchy",
        "toolReferenceName": "hivemindCalls",
        "tags": ["hivemind", "cpp", "c++", "call-hierarchy", "callers", "callees", "trace", "clangd", "ast", "code-graph"],
        "displayName": "C/C++ Call Hierarchy (AST-precise)",
        "modelDescription": "For a C/C++ function, returns who calls it (incoming) and/or what it calls (outgoing) — AST-precise via clangd. Supports `depth: 2` to trace two levels in a single call (e.g. callers-of-callers). Use this when tracing bugs across function boundaries or before changing a function's signature/contract to understand the upstream and downstream blast radius. Provide either `symbolName` (most common) or explicit `line` + `character`.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the C/C++ file containing the function."
            },
            "symbolName": {
              "type": "string",
              "description": "Name of the function (e.g. 'processRequest', 'MyClass::handle'). Either this OR (line + character) is required."
            },
            "line": {
              "type": "number",
              "description": "1-indexed line number of the cursor position. Required together with `character`."
            },
            "character": {
              "type": "number",
              "description": "0-indexed column number of the cursor position (LSP convention). Required together with `line`."
            },
            "direction": {
              "type": "string",
              "enum": ["incoming", "outgoing", "both"],
              "description": "incoming = who calls this | outgoing = what this calls | both (default)."
            },
            "depth": {
              "type": "number",
              "description": "How many levels deep to trace (1 or 2). Default 1. Use 2 sparingly — output grows quickly."
            },
            "maxPerLevel": {
              "type": "number",
              "description": "Cap on number of children at each level. Default 25."
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_typeHierarchy",
        "toolReferenceName": "hivemindTypes",
        "tags": ["hivemind", "cpp", "c++", "type-hierarchy", "inheritance", "class", "clangd", "ast", "code-graph"],
        "displayName": "C/C++ Type Hierarchy (AST-precise)",
        "modelDescription": "For a C/C++ class, struct, or interface, returns its supertypes (base classes) and/or subtypes (derived classes) via clangd. AST-precise — handles multiple inheritance and templates. Use BEFORE modifying any base class — every subtype returned may be affected. Combine with `hivemind_findOverrides` on individual virtual methods to find concrete implementations to update. Provide either `symbolName` (the class name) or an explicit `line` + `character` cursor position.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to the C/C++ file containing the class/struct/interface declaration."
            },
            "symbolName": {
              "type": "string",
              "description": "Name of the class, struct, or interface (e.g. 'IFoo', 'BaseWidget'). Either this OR (line + character) is required."
            },
            "line": {
              "type": "number",
              "description": "1-indexed line number of the cursor position. Required together with `character`."
            },
            "character": {
              "type": "number",
              "description": "0-indexed column number of the cursor position (LSP convention). Required together with `line`."
            },
            "direction": {
              "type": "string",
              "enum": ["supertypes", "subtypes", "both"],
              "description": "supertypes = bases | subtypes = derived | both (default)."
            },
            "depth": {
              "type": "number",
              "description": "How many levels deep to trace (1, 2, or 3). Default 2."
            },
            "maxPerLevel": {
              "type": "number",
              "description": "Cap on number of children at each level. Default 25."
            }
          },
          "required": ["filePath"]
        }
      },
      {
        "name": "hivemind_macroExpand",
        "toolReferenceName": "hivemindMacroExpand",
        "tags": ["hivemind", "cpp", "c++", "macro", "preprocessor", "clang", "code-graph"],
        "displayName": "C/C++ Macro Expansion (clang preprocessor)",
        "modelDescription": "Runs `clang -E` with the file's exact compile_commands.json flags and returns the actual preprocessor expansion of a specific source line. Use this when you need to know what `FCIMPL3(MyMethod, ...)` (or any nested/conditional macro) ACTUALLY becomes in this specific translation unit — `hivemind_findMacro` only shows the #define text and cannot account for #ifdef branches, -D flags, or recursive expansion. Required: `filePath` (must be a .cpp/.cc/.cxx — not a header), `line` (1-indexed). Optional: `contextLines` (default 0; ±N lines around `line`).",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Path to a C/C++ source file (.cpp/.cc/.cxx/.c). Headers are NOT supported — call this on a TU that #includes the header instead."
            },
            "line": {
              "type": "number",
              "description": "1-indexed line number to expand."
            },
            "contextLines": {
              "type": "number",
              "description": "Include ±N lines of expansion around the target line. Default 0. Cap 20."
            },
            "timeoutMs": {
              "type": "number",
              "description": "Timeout in milliseconds for the clang invocation. Default 30000."
            }
          },
          "required": ["filePath", "line"]
        }
      },
      {
        "name": "hivemind_buildSubset",
        "toolReferenceName": "hivemindBuildSubset",
        "tags": ["hivemind", "cpp", "c++", "build", "compile", "syntax-check", "clang", "code-graph"],
        "displayName": "C/C++ Build Subset (syntax-only)",
        "modelDescription": "Runs `clang -fsyntax-only` (or clang-cl `/Zs`) on every translation unit in the impact set of a seed file, using each TU's exact compile_commands.json flags. This is the cheapest way to answer 'did I break the build?' after editing a header — no codegen, no linking, no PDB locking. Returns pass/fail per TU plus compiler diagnostics. Required: `filePath` (the changed file). Optional: `maxTUs` (default 50, cap 500), `depth`, `perFileTimeoutMs`, `totalBudgetMs`, `parallelism`, `explicitTUs` (skip impact walk; compile exactly these TUs). Requires `compile_commands.json` and clang/clang-cl on the system.",
        "canBeReferencedInPrompt": true,
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Seed file (the one changed). Reverse-dependency walk starts here."
            },
            "maxTUs": {
              "type": "number",
              "description": "Cap on number of translation units to compile. Default 50. Above 200 is unwise on large repos."
            },
            "depth": {
              "type": "number",
              "description": "Reverse-dependency walk depth. Default 8."
            },
            "perFileTimeoutMs": {
              "type": "number",
              "description": "Per-TU compiler timeout in milliseconds. Default 60000."
            },
            "totalBudgetMs": {
              "type": "number",
              "description": "Total wallclock budget in milliseconds. Default 300000 (5 min)."
            },
            "parallelism": {
              "type": "number",
              "description": "Max concurrent compiler processes. Default min(8, CPU count)."
            },
            "explicitTUs": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Optional explicit list of TUs to compile. Skips the impact-graph walk; only these files are checked."
            }
          },
          "required": ["filePath"]
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/vscode": "^1.87.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0"
  }
}
